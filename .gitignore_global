CMakeFiles/
.*.d
*.swp
*.dSYM/
*.[oa]
*.dylib
*.cmake
*.zwc
*.pyc
*.un~
tags
.nvimlog
.claude/
.venv/
.DS_Store
# .envrc NOT ignored globally: the dotfiles repo itself tracks a .envrc
# (RIPGREP_CONFIG_PATH export). Projects that need to ignore .envrc should
# add it to their own .gitignore, not here.
.backpassrc.json

# Intentionally not ignored globally:
# - package-lock.json: library repos should commit lockfiles.
# - .reasonix/: dotfiles tracks .reasonix/config.toml; ignore runtime data per repo.
