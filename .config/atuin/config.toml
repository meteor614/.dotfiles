# Atuin config — only overrides vs defaults are kept here.
# Full annotated default: `atuin default-config` or
# https://github.com/atuinsh/atuin/blob/main/crates/atuin-client/config.toml

filter_mode_shell_up_key_binding = "session-preload"
ctrl_n_shortcuts = true
enter_accept = true

[sync]
records = true
