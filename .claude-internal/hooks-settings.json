{
  "_comment": "claude-internal → herdr agent state hooks. Custom (not herdr's official claude integration) so we can label panes 'claude-internal' instead of 'claude' to distinguish from the public claude. Mirrors herdr's official v5 model: a single SessionStart hook reports the session_id; herdr derives working/idle/release from terminal activity.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": "bash ~/.claude-internal/hooks/herdr-agent-state.sh session", "timeout": 10 }
        ]
      }
    ]
  }
}
